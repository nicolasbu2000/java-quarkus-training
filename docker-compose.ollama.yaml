services:
  ollama:
    image: ollama/ollama:0.30.0-rc28-rocm
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
    name: ollama_data